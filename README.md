## static-map-preview

### example

```js
var preview = require('static-map-preview')(d3)('mapbox.map-id');

                        // GeoJSON      w,   h
var container = preview(geoJsonObject, [200, 200]);
```
